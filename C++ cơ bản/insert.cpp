#include <bits/stdc++.h>
#include <set>
using namespace std;
int main ()
{
    set <int> s;
 s.insert (200);
 s.insert (724);
 s.insert (400);
 s.insert (234);
 s.insert (500);
 s.insert (934);
 cout << s.size () << endl;
 return 0;
}